<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".LoginActivity"
    android:background="@drawable/drawer_header_bg"
    android:layout_gravity="center">

    <ImageView
        android:id="@+id/logo_login"
        android:layout_width="@dimen/imagesize_iv_logolauncher_width"
        android:layout_height="@dimen/imagesize_iv_logolauncher_height"
        android:src="@drawable/logo_go4lunch_launcher"
        android:layout_marginTop="@dimen/activity_vertical_margin"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />

    <Button

        android:id="@+id/facebook_login_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/custom_facebook_button"
        android:padding="@dimen/nav_header_vertical_spacing"
        android:layout_margin="@dimen/nav_header_vertical_spacing"
        android:text="@string/facebook_login_btn_txt"
        android:textColor="@color/white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/logo_login" />

    <Button
        android:id="@+id/google_login_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/nav_header_vertical_spacing"
        android:background="@drawable/custom_google_button"
        android:padding="@dimen/nav_header_vertical_spacing"
        android:paddingVertical="14dp"
        android:text="@string/google_login_btn_txt"
        android:textColor="@color/white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/facebook_login_btn" />


</androidx.constraintlayout.widget.ConstraintLayout>